@model IEnumerable<FinalProject.UI.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
}

<h2>Admin Users</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>

            </th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", null, new { id = item.Id, @class = "btn btn-success" }) |
                    @Html.ActionLink("Details", "Details", null, new { id = item.Id, @class = "btn btn-info" }) |
                    @Html.ActionLink("Delete", "Delete", null, new { id = item.Id, @class = "btn btn-warning" })
                </td>
            </tr>
        }
    </tbody>

</table>

@section scripts {
    <script>
        $(document).ready(function () {
            $('#myTable').DataTable();
        });
    </script>
}